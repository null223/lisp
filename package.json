{
  "name": "lisp",
  "private": true,
  "scripts": {
    "deploy:hosting":   "firebase use lisp-null223 && firebase deploy --only hosting",
    "deploy:functions": "firebase use lisp-null223 && firebase deploy --only functions",
    "deploy:firestore": "firebase use lisp-null223 && firebase deploy --only firestore"
  },
  "devDependencies": {
    "firebase": "^7.14.0",
    "firebase-tools": "^8.0.2"
  }
}
